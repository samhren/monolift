# Database Configuration
DATABASE_URL=postgresql://monolift:monolift_dev@localhost:5432/monolift
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=monolift
DB_PASSWORD=monolift_dev
DB_DATABASE=monolift

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration (development keys - change for production)
JWT_SECRET=dev-jwt-secret-key-change-in-production
JWT_REFRESH_SECRET=dev-jwt-refresh-secret-key-change-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d